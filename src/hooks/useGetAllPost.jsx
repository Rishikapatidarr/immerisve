import { useEffect } from "react";

const useGetAllPost=()=>{
  useEffect(()=>{
    const fetchAllPost =async ()=>{
      try {
        
      } catch (error) {
        
      }
    }
    fetchAllPost();
  },[])
}

export default useGetAllMessage;